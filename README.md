# react mobx framework

react + react-router + mobx + webapck simple scaffold

init project

```bash
npm install 
```

start dev server

```bash
npm run dev 
```

build product 

```bash
npm run build
```
